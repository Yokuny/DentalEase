const SubtitleSeparator = ({ subtitle }: { subtitle: string }) => (
  <div className="w-full mt-4">
    <h4 className="text-darkBlue dark:text-skyBlue text-xs font-semibold">{subtitle}</h4>
  </div>
);

export default SubtitleSeparator;
