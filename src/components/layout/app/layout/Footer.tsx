import ThemeToggle from "@/components/theme/ThemeToggle";

const Footer = () => {
  return (
    <footer className="bg-transparent4 w-full h-48 flex flex-col items-center justify-evenly">
      <ThemeToggle />
    </footer>
  );
};

export default Footer;
